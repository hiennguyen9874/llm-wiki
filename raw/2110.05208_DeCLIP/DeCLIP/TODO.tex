% \paragraph{TODO(7.11-7.13)}

% \begin{itemize}
% \item Draw main workflow figure (SSL + Multi-View + NN).
% \item Finish first edition of introduction. Using 1-3 sentences to summarize the main concept of each paragraph. 
% \item Get the downstream baselines of CLIP (cls, det, seg).
% \item Train CLIP baseline on 30M/50M data (and evaluate on downstream tasks).
% \end{itemize}

% \paragraph{TODO(7.14-7.16)}

% \begin{itemize}
% \item Finish first edition of approach. 
% \item According to the intro/approach, write a clear experiments plan.
% \end{itemize}


% \paragraph{TODO(8.3-8.4)}

% \begin{itemize}
% \item Rewrite introduction.
% \item Run 28M CLIP experiments.
% \end{itemize}

% \paragraph{TODO(8.5-8.8)}

% \begin{itemize}
% \item Align the zero-shot validation in CLIP paper.
% \item Identify the effectiveness of SSL (SimSiam brings little improvement). 
% \item Fix downstream datasets \& get official CLIP's corresponding number.
% \item Grab more academic datasets, e.g. WIT.
% \item Write the first edition of approach.
% \end{itemize}


\paragraph{TODO(9.27)}

\begin{itemize}
    % \item Except for Language-Image in CLIP, we use image-text pairs. For any visual concepts, we use image. For any textual concepts, we use text.
    % \item How do you think the abstract? Our work IS an incremental work, such as roBERTa for BERT, DeiT for ViT, Deforamable DETR for DETR.
    \item Brain storm: Are there better names for self-supervision (SS), multi-view supervision (MVS), nearest neighbor supervision (NNS).
    % \item Exp in abstract: One sentence to show efficiency (R50/ViT). 2. One sentence to show downstream verification (9/10). 3. One sentence to show our work can scale up, no SOTA claim (RegNet-Y). Two reasons why we should put it up : 1. We actually have 2. Big tech companies might want to know, i.e. this result will convince them to use DeCLIP on their projects.
    \item \checkme{Whether the text encoder of CLIP scale up?}
    % \item \checkme{Our approach is double scaling up. Check the overall parameters of our RegNet-Y, CLIP R50x16 params.}
    \item \checkme{Check the config of CLIP R50x16.}
    % \item Redraw Fig.1, remove 3M experiments
    % \item Redraw Fig.2, maybe choose 10 out of 12? Refer GAIA paper(https://arxiv.org/pdf/2106.11346.pdf) for a good look. 
    % How to cheery pick? It is good if we can tell why we perform badly on these two datasets.
    \item Self-supervision for text. I think we need it to match (1) self-supervision within each modality;
    % \item \checkme{Fig.3 Not discriminative. Color deep. Text Language Modelling.}
    \item \checkme{Simsiam Text supervision}
    % \item Redraw Fig3 from detailed picture to skeleton?
    \item Scaling up models at Sec.3
    \item Ablation on Training Cost.
    \item Some visualization 1) attention map: our Declip can look more precisely at the object. 2) NN in dataset.
    % \item Format of tabel (Use ).
    % \item CLIP YFCC15M, our re-implementation.
    \item wikipedia query frequency top10w 
    \item \checkme{RegNetY-64GF: 276.237M/ResNetx64: 288M}
    \item \checkme{our work exceeds CLIP with releasing r50-ImageNet pretrain, exceeds some  CLIP CLIP with ViT-B/32-ImageNet21k pretrain, }
\end{itemize}

% \paragraph{TODO(9.30)}

% \begin{itemize}
%     \item For Fig.2, I look at the ImageNet results in the CLIP paper. It turns out that the performance of ImageNet pre-trained model is much lower than CLIP. If we draw it in Fig.2, Our gain might be not that significant. \lf{I suggest put the ImageNet performance at the table in experiments.}
    
    % \item Fig.2 need further improvements. Can we change 'DEC' to 'DeC'? How about the range of y-axis is either 4 (IN: 71-75, C100 68-72, PE 86-90, C10 87-91, SUN 70-74, AIR 46-50, DTD 74-78, Avg 77-81) or 6 (FL: 94-100, FO 81-87, CARS 77-83, CAL 89-95). The height of fig.2 should match that of fig.1 (shorter). We can also change the ratio of fig.1 and fig.2.
    
    

    % \item Best R50/ViT-B32 zero-shot. How to clarify our contribution to the community. \lf{Done: I put this part in last paragraph of intro.}
    
    % \item The introduction is somehow like related work, not a story. Rewrite it. \lf{Done: I re-wrote intro. once again on Sep.30. }
    % \end{itemize}

% \paragraph{TODO(10.1)}

% \begin{itemize}
    
    % \item According to fig.2 Our declip only outperforms clip in 8 out of 11 dataset (excluding average.) \lf{Done: Changing '9 in 11' to '8 in 11'. }
    
    % \item Rewrite related work. \lf{Done: I re-wrote related work on Oct.1. }
    
    % \item Fig.3 Not discriminative. Color deep. Text Language Modelling. \lf{Done: I re-drew Fig.3. }
    
    % \item We do not line up each components in the method part. Rewrite method. \lf{Half-way: I re-wrote till to Sec.3.2. Move Sec.3.3 to Oct.2. }
    
%     \item Original CLIP text encoder is not bidirectional, so how does the MLM (from bidirectional BERT) fit into this paradigm?
    
% \end{itemize}

\paragraph{TODO(10.2-3)} 
\begin{itemize}
    \item Rewrite Sec.3.3. \lf{Done: I re-worte Sec.3.3 on Oct.2.}
    
    \item The number in Fig.2 need to be a little bit larger.
    
    \item We should have CLIP-ImageNet results in Tab.3.
    
    \item Add image/text augmentations in Appendix.
    
    \item @lichen, I need you to fill the language SS in Sec 3.3.1.
    
    \item \checkme{A better order for Tab.4? It seems MVS brings most of the gain, how about SS - MVS - NNS?}
    
    \item Rewrite \& and reorganize exp and ablation study. \lf{Half-way to Sec4.1.}
    
    \item Put all the experiments, tables and figures in exp/ablation. Then I will rewrite them. 
    
    \item fig3. MLM \lf{Done: I added small circle above textual features to indidate MLL.}
    
    \item Appendix, more details about how to get the crawled data. Maybe provide some cost estimations.
    
    \item Missing: attention map of a image.
    
    \item Redraw Fig.9 x-axis should be time. It should have the declip curve as well. Suggest use the plot code of Fig.1
    
    \item Redraw Fig.6, how about 2 image-text pairs for CC/ YFCC/ Google? We could have some slight failure cases in this figure , we can explain it.
    
    \item double check eq.2
    
    \item Can we change order of SS and MVS?
    
    \item \checkme{Contribution, ablation on training cost, conclusion and appendix will be rewritten soon}
    
\end{itemize}



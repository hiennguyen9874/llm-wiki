# Wiki Lint — 2026-09-17

## State
- **Populated.** `wiki/index.md` (`okf_version: "0.2"`) and `wiki/log.md` are present and valid.
- 238 concepts, 1 index file. `python3 tools/wiki_check.py` → `OK: 238 concept(s), 1 index file(s)`.
- No missing reserved files, no unindexed concepts, no duplicate index entries, no broken local Markdown links, no unresolved local `resource`/`scope` paths after repair.

## Scope
- Full corpus audit: all 240 `wiki/*.md` files (238 concepts + `index.md` + `log.md`).
- Checked: required metadata, lifecycle values, index coverage/uniqueness, link targets, `sources[].id` ↔ body-citation ↔ footnote-definition joins, local source resolvability, metadata/index agreement, index sort, log heading reuse/ordering, relationship labels, contradictions/supersession context, privacy patterns, maturity/staleness, orphan reachability, single-source dependence.
- Direct file inspection for all 10 provenance-mismatch files; script-assisted enumeration for joins, index agreement, sorting, inbound links, and secret patterns across all 238 concepts.

## Visibility limits
- No external research performed; author-reported benchmarks treated as reported, not independently verified.
- No QMD cache consulted; retrieval validated on catalog + exact search only.
- Coarse-locator review sampled footnote definitions; full consequential-claim audit would require per-claim re-reading.
- Raw artifacts verified for existence/resolvability only, not re-ingested for factual re-validation.

## Counts
- Concepts: 238 (`type: Concept` 188, `type: Synthesis` 50).
- Status: `stable` 181, `draft` 57, `deprecated` 0 (all explicit; no absent-status pages).
- Index entries: 238, unique, complete.
- Single-source concepts: 107 (many secondary Vietnamese overviews without primary corroboration).
- Concepts with zero inbound concept-concept links (still index-reached, not orphaned): 5 — `causal-encoder-decoder-vs-transformer-architectures.md`, `deterministic-parameter-free-projection-for-linear-attention.md`, `dflash-vs-dspark-comparison.md`, `million-token-context-and-native-multimodality-claim-review.md`, `qwen3-8-2-4t-a95b-release-interface-and-evaluation-limits.md`.
- `## Contradictions` sections: 9 (all state competing claims with sources, no silent resolution observed).
- Privacy pattern hits: 0 (AWS key, private-key block, `sk-`, `gh*_` patterns; values redacted by design — only file/category reported, none found).

## Repairs (deterministic errors)
1. **Source/citation/footnote joins — 25 issues across 10 files, all repaired:**
   - `bloom-open-multilingual-language-model.md`: added missing `[^alibi-summary]` footnote definition.
   - `longcat-flash-lite-ngram-embedding-architecture.md`, `longcat-flash-lite-evaluation-deployment-and-release-limits.md`, `n-gram-embeddings-and-conditional-memory.md`: added missing `[^longcat-embedding-scaling-2026]` footnote (canonical: Hong Liu et al., LaTeX source, Abstract + Sections 2–6).
   - `direct-preference-optimization.md`: added missing `orpo-summary` source (`../raw/ORPO.md`).
   - `instructgpt-human-feedback-alignment.md`: added missing `dpo-summary` source (`../raw/DPO.md`).
   - `scaled-dot-product-and-multi-head-attention.md`: added missing `mqa-summary` source (`../raw/MQA.md`).
   - `flashattention-implementation-evolution.md`: added `mqa-summary` source; removed unused `flashattention-2-summary` source (primary `flashattention-2-2023` already supports FA2 claims).
   - `flashattention-io-aware-exact-attention.md`: removed unused `flashattention-summary` source (all claims cite primary `flashattention-2022`).
   - `sparse-attention-evolution-and-architecture-comparison.md`: renamed 7 `sources[].id` to match body/footnote labels (`user-map`, `moba`, `deepseek-dsa`, `qwen-qsa`, `longcat-lsa`, `deepseek-v4`, `kimi-linear`); removed unused `qwen38-next-modeling` source (its implementation file remains linked inside `[^qwen-qsa]` definition).
2. **Metadata/index agreement — 3 entries fixed to exact frontmatter title/description:**
   - `delta-rule-vs-ssm-frontier-adoption.md` (Vietnamese description), `kv-caching.md` (straight apostrophe), `long-context-architecture-archetypes-beginners-course.md` (full title + description).
3. **Index ordering:** re-sorted all 238 `## Concepts` entries alphabetically by title (case-insensitive); fixed 28 out-of-order pairs. `wiki_check.py` still `OK`.
4. **Log heading reuse:** merged two adjacent `## 2026-08-21` blocks into one (preserved bullet order); no history reordering.

## Unresolved findings
### Errors
- None remaining. Structural check, joins, links, source resolvability, index coverage/uniqueness/agreement, and privacy all pass.

### Warnings (human review preserved, no auto-mutation)
- **Log not newest-first (immutable history preserved):** `## 2026-11-16` sits mid-file (after `2026-08-20`, before `2026-08-15`) and matches future-dated `expert-parallelism-serving-trade-offs-beginners-guide.md` (`created: 2026-11-16`; today is 2026-09-17). `## 2026-08-12` appears twice non-adjacently (after `2026-08-13` and after `2026-08-10`). Reordering would rewrite immutable history; needs human decision (correct future date vs. accept ordering note).
- **nanoGPT lifecycle inconsistency:** `nanogpt-gpt-2-reference-implementation.md` is `status: stable` but its description says “now-deprecated” and body says the source labels the repo old/deprecated with README redirect to nanochat; reciprocal `Supersedes`/`Superseded` relationship bullets exist but there is no `## Supersession` section. Semantic status change (`stable` → `deprecated`) preserved for human review.
- **Single-source trust surface:** 107 single-source concepts. Central secondary-only pages (e.g., BLOOM, Chinchilla, Constitutional AI, InstructGPT/DPO summaries) remain `draft` appropriately; no auto-promotion performed. Prioritize primary ingestion for most-reused claims rather than bulk status changes.
- **Typed relationships:** corpus uses an open vocabulary (`Evaluates`, `Extends`, `Implements`, `Elaborates`, `Uses`, etc.) beyond the contract’s examples. All sampled bullets carry supporting prose/citations, so no unsupported-relationship warning filed; contract wording (“such as … open vocabulary”) covers this, but a clarifying note could prevent future false positives.

### Suggestions
- Add `stale_after` to serving/deployment limits with version-pinned stacks (e.g., LongCat SGLang examples, Transformers 4.57.6+/PyTorch 2.6+ claims) if version drift matters; otherwise omit per contract.
- Add section-level locators to purely secondary survey footnotes (e.g., `[^transformer-architecture-survey]` in `architecture-map-and-attention-masks-beginners-course.md` cites `../raw/TongHopKienTrucTransformer.md` with no section); low priority unless consequential claims depend on it.
- Consider inbound bidirectional context for the 5 index-reached but concept-unlinked pages where useful; not orphaned per contract (index reaches them), so enrichment only.
- No new-concept recommendation meets the “repeated or central mentions” threshold in this audit; no external source research performed.

## Validation
- `python3 tools/wiki_check.py` → `OK: 238 concept(s), 1 index file(s)` before and after repairs.
- Post-repair joins script → 0 mismatches; index-agreement script → 0 mismatches; index sort check → 0 out-of-order pairs; local `resource`/`scope` resolvability → 0 missing; secret-pattern scan → 0 hits.
- Changed files (12): 10 concepts + `wiki/index.md` + `wiki/log.md` (this report excludes its own `Lint` log entry, added next per contract).

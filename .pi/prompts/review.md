---
description: Run weekly or monthly second-brain review
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-maintenance`, `qmd`, and `obsidian-markdown`. Use `purpose.md`, `wiki/overview.md`, and `wiki/index.md` when orientation is helpful. Review open `outputs/review-queue/` items, `raw/inbox/`, `raw/captures/`, recent crystallizations, stale drafts, pages marked `needs_update`, under-integrated source pages, and durable outputs that should be promoted into the wiki. Use QMD to find weakly connected, stale, disputed, low-quality, low-confidence, isolated, bridge, or sparse-cluster areas. Run the maintenance workflow with retention review, contradiction resolution where warranted, graph-insights-lite, and a light privacy scan over recent downstream artifacts. Refresh lifecycle metadata and visibility when justified, mark supersession where supported, crystallize important recent exploratory work, update `wiki/overview.md` if current state changed, update markdown pages, and if Bases or Canvases need work activate `llm-wiki-visualization` with the relevant visualization skill. Append review/update entries to `wiki/log.md` with what changed and why."

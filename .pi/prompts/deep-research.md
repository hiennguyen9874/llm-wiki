---
description: Plan manual-first deep research from wiki gaps
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-query`, `llm-wiki-ingest`, `qmd`, and `obsidian-markdown`. Read `purpose.md` and `wiki/overview.md` unless the scope is narrow. Use QMD to inspect relevant wiki pages, prior outputs, and gaps. Produce a manual-first Deep Research plan: research objective, why it matters to purpose, existing knowledge, key gaps, recommended search queries, candidate source types, and acceptance criteria for sources. Ask the user to approve or provide URLs before ingesting. Do not claim automated web search or auto-ingest unless explicitly available. If the plan itself is durable, save it in `outputs/analyses/` or create review queue items in `outputs/review-queue/`, then log meaningful saved artifacts."

User Request: $ARGUMENTS

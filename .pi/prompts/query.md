---
description: Query the knowledge base
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-query`, `qmd`, and `obsidian-markdown`. Use `wiki/index.md` for orientation only when helpful. Use QMD to find relevant pages, expand from the first results using entities, metadata, and typed relationships, read the relevant pages, and answer: $ARGUMENTS. Cite which pages informed the answer and mention if key claims are low-confidence, stale, disputed, or superseded. If the answer creates durable value, save it in `outputs/answers/` or promote it into `wiki/`. If a Canvas or Base would materially help, activate `llm-wiki-visualization` plus the relevant visualization skill, then update the index and log as needed."

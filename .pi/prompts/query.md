---
description: Query the knowledge base
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-query`, `qmd`, and `obsidian-markdown`. Use `purpose.md` and `wiki/overview.md` for broad, strategic, gap, research-planning, or session-orientation questions; use `wiki/index.md` for orientation only when helpful. Use QMD for both exact-term and concept-level retrieval, expand from the first results using entities, metadata, typed relationships, backlinks, and supersession links, read the relevant pages, and answer user request. Cite which pages informed the answer, distinguish supported facts from inference and uncertainty, and mention if key claims are low-confidence, stale, disputed, or superseded. Persist the result only if it creates durable reusable value, is well cited, and is not duplicative; then save it in `outputs/answers/` or promote it into `wiki/`, set visibility appropriately, and log the saved artifact. If a Canvas or Base would materially help, activate `llm-wiki-visualization` plus the relevant visualization skill, then update the index and log as needed."

User Request: $ARGUMENTS
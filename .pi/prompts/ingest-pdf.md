---
description: Ingest a PDF document into the second brain
---

Read `AGENTS.md`. Activate `llm-wiki-core`, `llm-wiki-ingest`, `pdf`, `qmd`, and `obsidian-markdown`.

Process $ARGUMENTS as a PDF source. Preserve or copy the raw PDF into the appropriate `raw/` location, use the `pdf` skill for extraction/rendering when layout matters, then continue with the normal Ingest Workflow in `llm-wiki-ingest`.

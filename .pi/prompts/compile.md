---
description: Compile raw sources or durable outputs into canonical wiki pages
---

Read `AGENTS.md`. Activate `llm-wiki-core`, `llm-wiki-ingest`, `llm-wiki-query`, `llm-wiki-maintenance`, `qmd`, and `obsidian-markdown`.

Use the Compile Workflow in `llm-wiki-ingest` for: $ARGUMENTS

Determine scope from the request. If unclear, ask one focused question before editing. Do not claim automated queue, graph engine, vector database, or auto-ingest behavior unless those tools are actually available and used.

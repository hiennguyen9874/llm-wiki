---
description: Find the three biggest gaps in the knowledge base
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-query`, `qmd`, and `obsidian-markdown`. Read `purpose.md` and `wiki/overview.md` unless scope is narrow. Identify the three biggest gaps in this knowledge base: topics that are mentioned or implied but lack dedicated pages, areas where coverage is shallow, important concepts that are missing, or graph-insights-lite issues such as isolated pages, sparse clusters, or bridge pages without synthesis. For each gap, explain why it matters, which current pages reveal the gap, what a future page should cover, and what manual-first Deep Research queries or source types would help. If surfacing these gaps reveals durable connections worth preserving, create or update a page in `wiki/` or save an artifact in `outputs/analyses/`, then update the index and log as needed."

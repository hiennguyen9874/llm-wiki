---
description: Ingest an arXiv paper into the second brain
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-ingest`, `read-arxiv-paper`, `qmd`, and `obsidian-markdown`. Treat $ARGUMENTS as an arXiv URL. Use the `read-arxiv-paper` workflow to normalize the URL to the `/src/` archive, download or reuse the cached source bundle, unpack it, locate the LaTeX entrypoint, and read the paper from source files rather than the PDF when possible. Preserve the raw paper source under `raw/papers/` with a stable arXiv-ID-based name and keep the original arXiv URL in source metadata. Do not write the skill's default `./knowledge/summary_*.md`; instead continue with the normal two-stage ingest workflow from the ingest skill: screen for sensitive material before promoting anything into `wiki/` or `outputs/`, use `purpose.md`, `wiki/overview.md`, and `wiki/index.md` when broad orientation helps, find related pages, produce a stage-1 ingest plan for substantial papers, save audit-worthy plans in `outputs/ingest-plans/`, create review items in `outputs/review-queue/` for non-blocking human judgments, extract entities, typed relationships, and candidate claims, assess reinforcement or supersession, resolve contradictions with the ingest rubric, update the source page and related canonical/overview pages, add lightweight `Evidence / claims` records where useful, refresh lifecycle metadata and visibility, run a quality self-check, consider whether a Base or Canvas should be updated, update `wiki/index.md`, and append to `wiki/log.md` with what changed and why."

---
description: Ingest a web URL into the second brain
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-ingest`, `defuddle`, `qmd`, and `obsidian-markdown`. Use Defuddle for the provided URL unless it ends with `.md`. Save the cleaned markdown into `raw/web-clips/` with a stable kebab-case filename, preserve the original URL in source metadata, then run the normal ingest workflow from the ingest skill: find related pages, extract entities and typed relationships, assess reinforcement or supersession, update the source page and related canonical pages, consider whether a Base or Canvas should be updated, update `wiki/index.md`, and append to `wiki/log.md`."

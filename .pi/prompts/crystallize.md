---
description: Distill a completed research/debugging/exploration thread into durable memory
---

"Read AGENTS.md. Then activate `llm-wiki-core`, `llm-wiki-crystallize`, `qmd`, and `obsidian-markdown`. Crystallize $ARGUMENTS into a structured digest in `outputs/crystallizations/` or `outputs/analyses/`. Capture the original question, what was investigated, key findings, affected files/entities/pages, unresolved questions, and reusable lessons. Then promote the durable lessons into `wiki/` where appropriate, update `wiki/index.md` if needed, and append `crystallize` or `update` entries to `wiki/log.md`."

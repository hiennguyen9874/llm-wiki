---
description: Load relevant context at the start of a session
---

Read `AGENTS.md`. Activate `llm-wiki-core`, `llm-wiki-query`, `qmd`, and `obsidian-markdown`.

Use the Query Workflow in `llm-wiki-query` to load compact working context for $ARGUMENTS, or recent activity if no scope is given. Include current focus, key relevant pages, important recent changes, unresolved questions, and suggested next steps. Do not create or update durable artifacts unless explicitly asked.
